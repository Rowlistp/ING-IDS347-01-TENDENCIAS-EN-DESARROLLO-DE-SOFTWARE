services:
  mailpit:
    image: axllent/mailpit:v1.30.0
    ports:
      - "127.0.0.1:1025:1025"
      - "127.0.0.1:8025:8025"
    # Captura local únicamente; no SMTP relay ni entrega a direcciones externas.
